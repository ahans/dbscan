load("@pybind11_bazel//:build_defs.bzl", "pybind_extension")

pybind_extension(
    name = "dbscan_cpp",
    srcs = ["dbscan.cpp"],
    visibility = ["//visibility:public"],
    deps = [
        "@nanoflann",
    ],
)
